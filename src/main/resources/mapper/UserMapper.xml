<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.microtao.dao.UserMapper">

<select id="queryUserByUsername" resultType="com.smiletimli.mybatis_plus_demo.entity.User">
    select * from t_user where user_name = ${user_name}
</select>

</mapper>